<header class = "navbar navbar-fixed-top navbar-inverse">
	<div class ="container">
		<% if logged_in? %>
			<%= link_to "Build", users_home_path(@current_user), id: "logo" %>
		<% else  %>
		<% end %>
		<nav>
			<ul class = "nav navbar-nav navbar-right">
				<% if logged_in? %>
					<li><%= link_to "Food", users_food_path(@current_user) %></li>
					<li><%= link_to "Goals", users_goals_path(@current_user) %></li>
					<li><%= link_to "Log Out", logout_path %></li>
				<% else  %>
				<% end %>		
			</ul>
		</nav>
	</div>
</header>
